const HomePage = () => {
    return (
        <div>
            <p>
                This is a work in progress page. Check search + products + product page :)
            </p>
        </div>
    )
}

export default HomePage